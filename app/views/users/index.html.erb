<h1>Users</h1>
<%= will_paginate %>
<% @users.each do |user| %>
  <div class="row">
    <div class="" style = "width : 30%">
      <div class="img-circle" style = "float:left, height:100%;">
        <%= link_to (gravatar_for user) , user_path(user)%>
      </div>
      <span style = "float: left"><%=link_to user.username, user_path(user) %></span><br>
      <span style = "float: left"><%= user.email %></span>
      <div style = "float: right">
        <%= link_to 'Delete User', user_path(user), method: :delete, class: "btn btn-danger" if !user.admin?%>
      </div>
    </div>
  </div>

<% end %>


<%= will_paginate %>
